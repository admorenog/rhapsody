({
	app : {
		debug : "debug mode ${ vars.ctx.app.debug }"
	},
	menu : {
		open : "Open"
	},
	conductor : {
		errors : {
			command : {
				model : {
					arg_required : "You need to write the model name as argument, write ./conductor help make:model for more information about this command."
				}
			}
		}
	}
})